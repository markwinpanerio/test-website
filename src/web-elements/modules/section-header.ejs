<%  
    var alignmentModifier;

    if(typeof modifier == 'undefined') {
        alignmentModifier = 'text-center';
    } else {
        alignmentModifier = modifier;
    }
%>
<div class="section-header <%= alignmentModifier %>">
    <em class="js-section-header-item"><%= upperText %></em>
    <h2 class="h1 js-section-header-item"><%= title %></h2>
    <% if(typeof subTitle !== 'undefined') { %>
    <h3 class="h5 js-section-header-item"><%= subTitle %></h3>
    <% } %>
</div>